<div class="container">
  <div class="pic">
    <h1>All the Lists</h1>
  </div>
</div>
<ul>
  <% @lists.each do |list| %>
  <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
</ul>

<%= link_to "Create a list", new_list_path %>
